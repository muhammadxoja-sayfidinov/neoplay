import 'package:flutter/material.dart';

class CatalogSceen extends StatelessWidget {
  const CatalogSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
